import React from "react";

type Props = {};

const ViewMoreComment = (props: Props) => {
  return (
    <div className="mt-4">
      <p className="hover:text-gray-500 cursor-pointer">Xem thêm bình luận</p>
    </div>
  );
};

export default ViewMoreComment;
